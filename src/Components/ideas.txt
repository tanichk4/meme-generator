to do:

- should message and error be unified? or error state lifted to MemeGenerator.js and a setter function passed to useFetchImage?
- should useFetchImage return only an image, function to fetch image - or both?
- styling? 
- should isDuplicate be a separate function? 